ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [net.chrissearle.ApplicationKt.module]
  }
}

spoolman {
  spools = "/api/v1/export/spools?fmt=json"
  spools = ${?SPOOLMAN_SPOOLS_URL}

  spoolPrefix = "/spool/show/"
  spoolPrefix = ${?SPOOLMAN_SPOOL_PREFIX}
}
